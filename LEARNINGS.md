# ECC Self-Improvement Learnings Log
# Format: one JSON object per line — append only, do NOT edit existing lines
# Pruned monthly by ecc-prune (removes entries older than 30 days with confidence=low)
# Mined by ecc-self-improve to find systematic weaknesses (frequency >= 3)
